```mermaid
sequenceDiagram
    participant T1 as 测试用例 01
    participant T2 as 测试用例 02
    participant Barrier as 同步屏障 (Redis/Coordinator)
    participant System as 待测系统 (清算逻辑)

    Note over T1, T2: 第一阶段：执行清算前数据准备
    T1->>T1: 执行 Pre-check
    T2->>T2: 执行 Pre-check

    Note over T1, T2: 进入同步等待点
    T1->>Barrier: 报告就绪 (count=1)
    activate Barrier
    T2->>Barrier: 报告就绪 (count=2/Total)
    
    Note right of Barrier: 监测到所有用例已就绪
    Barrier->>System: 触发系统清算指令
    activate System
    System-->>Barrier: 清算完成响应
    deactivate System

    Barrier-->>T1: 释放信号 (Green Light)
    Barrier-->>T2: 释放信号 (Green Light)
    deactivate Barrier

    Note over T1, T2: 第二阶段：执行清算后验证
    T1->>T1: 执行 Post-check
    T2->>T2: 执行 Post-check